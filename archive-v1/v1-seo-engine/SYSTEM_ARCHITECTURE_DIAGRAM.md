# NLP Entity & Topic Coverage - System Architecture

Visual representation of the system architecture and data flow.

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    SEO Article Engine                        │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │         NLP Entity & Topic Coverage System         │    │
│  │                                                     │    │
│  │  ┌──────────────┐      ┌──────────────────┐       │    │
│  │  │   Entity     │      │  Topic Coverage  │       │    │
│  │  │  Extractor   │◄────►│    Analyzer      │       │    │
│  │  └──────────────┘      └──────────────────┘       │    │
│  │         │                       │                  │    │
│  │         └───────────┬───────────┘                  │    │
│  │                     │                              │    │
│  │              ┌──────▼──────┐                       │    │
│  │              │  API Route  │                       │    │
│  │              │   /analyze  │                       │    │
│  │              │  -entities  │                       │    │
│  │              └──────┬──────┘                       │    │
│  │                     │                              │    │
│  └─────────────────────┼──────────────────────────────┘    │
│                        │                                   │
│              ┌─────────▼──────────┐                        │
│              │  Frontend UI/CLI   │                        │
│              └────────────────────┘                        │
└─────────────────────────────────────────────────────────────┘
                         │
                         │ API Calls
                         ▼
              ┌──────────────────┐
              │  Google Gemini   │
              │   AI (2.0 Flash) │
              └──────────────────┘
```

## 📊 Data Flow Diagram

```
Input (Article + Keyword + Competitors)
           │
           ▼
┌──────────────────────────┐
│  1. Entity Extraction    │
│  - Extract your entities │
│  - Classify types        │
│  - Count mentions        │
│  - Score importance      │
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────────┐
│  2. Competitor Analysis      │
│  - Extract competitor entities│
│  - Build entity frequency map │
│  - Identify popular entities  │
└───────────┬──────────────────┘
            │
            ▼
┌──────────────────────────────┐
│  3. Entity Merging           │
│  - Merge your + competitor   │
│  - Add competitor mentions   │
│  - Identify missing entities │
└───────────┬──────────────────┘
            │
            ▼
┌──────────────────────────────┐
│  4. Subtopic Analysis        │
│  - Extract subtopics         │
│  - Analyze coverage depth    │
│  - Calculate competitor %    │
└───────────┬──────────────────┘
            │
            ▼
┌──────────────────────────────┐
│  5. Semantic Keyword Extract │
│  - Extract LSI keywords      │
│  - Score relevance           │
│  - Track frequency           │
└───────────┬──────────────────┘
            │
            ▼
┌──────────────────────────────┐
│  6. Completeness Scoring     │
│  - Entity score (40%)        │
│  - Subtopic score (40%)      │
│  - Keyword score (20%)       │
│  - Total: 0-100              │
└───────────┬──────────────────┘
            │
            ▼
┌──────────────────────────────┐
│  7. Placement Suggestions    │
│  - For missing entities      │
│  - Natural section placement │
│  - Context recommendations   │
└───────────┬──────────────────┘
            │
            ▼
┌──────────────────────────────┐
│  8. Recommendations          │
│  - Prioritize improvements   │
│  - Identify gaps             │
│  - Generate action items     │
└───────────┬──────────────────┘
            │
            ▼
Output (TopicCoverage + Recommendations + Summary)
```

## 🔄 Component Interaction

```
┌─────────────────────┐
│  entity-extractor   │
├─────────────────────┤
│ extractEntities()   │────┐
│ extractCompetitor   │    │
│ Entities()          │    │
│ mergeEntityData()   │    │  Used by
│ suggestEntity       │    │  ────────►
│ Placement()         │    │
│ determine           │    │
│ CoverageLevel()     │    │
└─────────────────────┘    │
                           │
                           │
┌─────────────────────┐    │
│  topic-coverage     │◄───┘
├─────────────────────┤
│ analyzeTopicCoverage│────┐
│ analyzeRequired     │    │
│ Subtopics()         │    │  Returns
│ extractSemantic     │    │  ────────►
│ Keywords()          │    │
│ calculate           │    │
│ CompletenessScore() │    │
│ generateCoverage    │    │
│ Recommendations()   │    │
└─────────────────────┘    │
                           │
                           │
┌─────────────────────┐    │
│  API Route          │◄───┘
├─────────────────────┤
│ POST handler        │
│ - Validate input    │
│ - Call analyzer     │
│ - Format response   │
│ - Error handling    │
│                     │
│ GET handler         │
│ - Return API docs   │
└─────────────────────┘
```

## 🎯 Function Call Hierarchy

```
POST /api/seo/analyze-entities
    │
    ├─► Validate Input
    │   ├─► Check articleContent
    │   ├─► Check keyword
    │   └─► Check competitorArticles
    │
    ├─► analyzeTopicCoverage()
    │   │
    │   ├─► stripHtml()
    │   │
    │   ├─► extractEntities(yourArticle)
    │   │   └─► Google AI API call
    │   │       └─► determineCoverageLevel()
    │   │
    │   ├─► extractCompetitorEntities(competitors)
    │   │   └─► Google AI API call
    │   │
    │   ├─► mergeEntityData()
    │   │
    │   ├─► analyzeRequiredSubtopics()
    │   │   └─► Google AI API call
    │   │
    │   ├─► extractSemanticKeywords()
    │   │   └─► Google AI API call
    │   │
    │   ├─► calculateCompletenessScore()
    │   │   ├─► Entity score (40%)
    │   │   ├─► Subtopic score (40%)
    │   │   └─► Keyword score (20%)
    │   │
    │   ├─► calculateCompetitorAverage()
    │   │
    │   └─► addSuggestedPlacements()
    │       └─► suggestEntityPlacement() for each missing entity
    │           └─► Google AI API call
    │
    ├─► generateCoverageRecommendations()
    │   ├─► identifyMissingSubtopics()
    │   ├─► identifyMissingKeywords()
    │   ├─► identifyUnderUtilizedKeywords()
    │   └─► calculateCoverageGaps()
    │
    └─► Format Response
        ├─► TopicCoverage object
        ├─► Recommendations array
        └─► Summary metrics
```

## 📦 Module Dependencies

```
entity-extractor.ts
    │
    ├─► @google/generative-ai
    ├─► ./seo-types (Entity interface)
    └─► ./constants (MIN_ENTITY_MENTIONS)

topic-coverage.ts
    │
    ├─► @google/generative-ai
    ├─► ./seo-types (TopicCoverage, Entity)
    ├─► ./entity-extractor (all functions)
    └─► ./constants (all NLP constants)

route.ts
    │
    ├─► next/server (NextRequest, NextResponse)
    ├─► @/lib/topic-coverage (all functions)
    └─► @/lib/seo-types (TopicCoverage)
```

## 🔀 Type Flow

```
┌──────────────┐
│  Raw Article │
│  (HTML)      │
└──────┬───────┘
       │
       │ stripHtml()
       ▼
┌──────────────┐
│  Plain Text  │
└──────┬───────┘
       │
       │ extractEntities()
       ▼
┌──────────────────────┐
│  EntityExtraction    │
│  Result              │
│  {                   │
│    entities: [{      │
│      name,           │
│      type,           │
│      mentions,       │
│      importance      │
│    }]                │
│  }                   │
└──────┬───────────────┘
       │
       │ determineCoverageLevel()
       ▼
┌──────────────────────┐
│  Entity[]            │
│  {                   │
│    name,             │
│    type,             │
│    mentions,         │
│    coverage,         │─────┐
│    importance,       │     │
│    competitorMentions│     │ mergeEntityData()
│  }                   │     │
└──────────────────────┘     │
                             │
┌──────────────────────┐     │
│  Competitor          │     │
│  EntityMap           │─────┘
│  Map<name, count>    │
└──────────────────────┘
       │
       │ + Subtopics + Keywords
       ▼
┌──────────────────────┐
│  TopicCoverage       │
│  {                   │
│    mainTopic,        │
│    entities,         │
│    requiredSubtopics,│
│    semanticKeywords, │
│    completeness,     │
│    competitorAverage │
│  }                   │
└──────────────────────┘
```

## 🎨 Scoring Algorithm

```
Completeness Score Calculation
═══════════════════════════════

┌─────────────────────────────────┐
│  Entity Coverage Score (40%)    │
├─────────────────────────────────┤
│  1. Filter important entities   │
│     (importance >= 50)          │
│  2. Count covered entities      │
│     (coverage = explained or    │
│      detailed)                  │
│  3. Calculate ratio             │
│     covered / total * 40        │
└────────────┬────────────────────┘
             │
             ├──────────────────┐
             │                  │
┌────────────▼────────────────┐ │
│ Subtopic Coverage Score     │ │
│ (40%)                       │ │
├─────────────────────────────┤ │
│ 1. Count covered subtopics  │ │
│    (depth = medium or deep) │ │
│ 2. Calculate ratio          │ │
│    covered / total * 40     │ │
└────────────┬────────────────┘ │
             │                  │
             ├──────────────────┘
             │
┌────────────▼────────────────┐
│ Semantic Keyword Score      │
│ (20%)                       │
├─────────────────────────────┤
│ 1. Filter relevant keywords │
│    (relevance >= 50)        │
│ 2. Count present keywords   │
│    (frequency >= suggested  │
│     * 0.6)                  │
│ 3. Calculate ratio          │
│    present / total * 20     │
└────────────┬────────────────┘
             │
             ▼
     ┌───────────────┐
     │  Total Score  │
     │  (0-100)      │
     └───────────────┘
```

## 🔄 Processing Pipeline

```
Step 1: Input Processing
┌─────────────────────────┐
│ Raw Inputs              │
│ - Article HTML          │
│ - Keyword               │
│ - Competitor HTMLs      │
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐
│ Validation              │
│ - Check lengths         │
│ - Validate types        │
│ - Limit competitors (5) │
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐
│ HTML Stripping          │
│ - Remove scripts/styles │
│ - Extract plain text    │
│ - Preserve structure    │
└───────┬─────────────────┘
        │
        ▼

Step 2: Entity Analysis
┌─────────────────────────┐
│ Your Article Analysis   │
│ - Extract entities      │
│ - Classify types        │
│ - Count mentions        │
│ - Score importance      │
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐
│ Competitor Analysis     │
│ - Extract entities      │
│ - Aggregate mentions    │
│ - Build frequency map   │
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐
│ Entity Merging          │
│ - Combine datasets      │
│ - Add competitor counts │
│ - Identify gaps         │
└───────┬─────────────────┘
        │
        ▼

Step 3: Topic Analysis
┌─────────────────────────┐
│ Subtopic Extraction     │
│ - Identify topics       │
│ - Analyze depth         │
│ - Calculate competitor% │
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐
│ Semantic Keyword Ext    │
│ - Extract LSI keywords  │
│ - Score relevance       │
│ - Track frequency       │
└───────┬─────────────────┘
        │
        ▼

Step 4: Scoring & Recommendations
┌─────────────────────────┐
│ Completeness Scoring    │
│ - Entity: 40%           │
│ - Subtopic: 40%         │
│ - Keyword: 20%          │
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐
│ Gap Analysis            │
│ - Missing entities      │
│ - Missing subtopics     │
│ - Missing keywords      │
│ - Score differential    │
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐
│ Placement Suggestions   │
│ - For missing entities  │
│ - Suggest sections      │
│ - Provide context       │
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐
│ Recommendation Gen      │
│ - Prioritize actions    │
│ - Rank by impact        │
│ - Format as list        │
└───────┬─────────────────┘
        │
        ▼

Step 5: Output
┌─────────────────────────┐
│ Response Assembly       │
│ - TopicCoverage         │
│ - Recommendations       │
│ - Summary metrics       │
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐
│ JSON Response           │
│ - Status: 200           │
│ - Content-Type: JSON    │
│ - Body: analysis        │
└─────────────────────────┘
```

## 🎯 Coverage Level Decision Tree

```
Entity Mentions
       │
       ├─► 0 mentions
       │   └─► coverage: "missing"
       │
       ├─► 1-2 mentions
       │   └─► coverage: "mentioned"
       │
       ├─► 3-5 mentions
       │   └─► coverage: "explained"
       │
       └─► 6+ mentions
           └─► coverage: "detailed"
```

## 🌐 System Integration Points

```
┌──────────────────────────────────────────┐
│        SEO Article Engine                │
│                                          │
│  ┌────────────────────────────────┐     │
│  │  Step 2: Research              │     │
│  │  - Competitor scraping         │─────┼──► Competitor HTMLs
│  └────────────────────────────────┘     │
│                                          │
│  ┌────────────────────────────────┐     │
│  │  Step 3: Framework             │     │
│  │  - Outline generation          │─────┼──► Article structure
│  └────────────────────────────────┘     │
│                                          │
│  ┌────────────────────────────────┐     │
│  │  Step 4: Article Generation    │     │
│  │  - Draft creation              │─────┼──► Article content
│  └────────────────────────────────┘     │
│                                          │
│           │                              │
│           └──────────────┐               │
│                          ▼               │
│  ┌────────────────────────────────┐     │
│  │  NLP Entity & Topic Coverage   │     │
│  │  - Analyze draft               │     │
│  │  - Identify gaps               │     │
│  │  - Suggest improvements        │     │
│  └────────────┬───────────────────┘     │
│               │                          │
│               ▼                          │
│  ┌────────────────────────────────┐     │
│  │  Step 5: Revision              │     │
│  │  - Apply recommendations       │     │
│  │  - Enhance coverage            │     │
│  └────────────────────────────────┘     │
│                                          │
└──────────────────────────────────────────┘
```

---

This architecture ensures a robust, scalable, and maintainable system for comprehensive topic coverage analysis.
